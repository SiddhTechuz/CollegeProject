header.header 
    nav.nav.nav--tours
        a.nav__el(href='/') FixIt Friend
    nav.nav.nav--user
        a.nav__el(href='/services') Services
        a.nav__el Testimonials 
        a.nav__el AboutUs
        | &nbsp;&nbsp;&nbsp;
        | &nbsp;&nbsp;&nbsp;
    if user
        a.nav__el(href='/add-task') Add Tasks
        a.nav__el.nav__el--logout LogOut
        a.nav__el(href='/me') 
            img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
            span= user.name.split(' ')[0] 
    else
        a.nav__el(href='/login') LogIn
        a.nav__el.nav__el--cta(href='/signup') SignUp           
